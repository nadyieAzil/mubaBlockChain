{
  "name": "suipact-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "SuiPact — Zero-Gas Stablecoin Escrow & Atomic Split Payouts on Sui",
  "scripts": {
    "dev:backend": "cd backend && npm start",
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "test:contracts": "cd contracts/suipact_escrow && sui move test",
    "build:contracts": "cd contracts/suipact_escrow && sui move build"
  }
}
